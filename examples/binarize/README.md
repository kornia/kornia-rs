Example showing to binarize an image using the Kornia Rust image processing library.

```bash
Usage: binarize --image-path <IMAGE_PATH>

Options:
  -i, --image-path <IMAGE_PATH>
  -h, --help                     Print help
```

Output:

TODO: add image output
